# Project Steward Attention UI Bridge

This UI-host companion lets Project Steward windows on the same machine exchange bounded AI-session attention state through profile-local files. It contains no user-facing commands and records no conversation content, prompts, responses, hostnames, remote authorities, or absolute project paths.

Install this extension locally before installing Project Steward in local, SSH, WSL, or Dev Container extension hosts.
